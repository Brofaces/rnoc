<div class="container">

  	<div class="navbar">
    	<div class="navbar-inner">
      		<div class="container">
				<a class="brand" href="#">
	  				RNOC
					</a>
      		</div>
    	</div>
  	</div>

	<% @pages.each do |page| %>
		<p><i class="icon-fire"></i> &nbsp; <%= link_to page.title, page_path(page) %></p>
	<% end %>
	
	<%= link_to 'New page', new_page_path %>
</div>
