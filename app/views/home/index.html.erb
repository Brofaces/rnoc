<div class="container">
  <% @pages.each do |page| %>
    <p><i class="icon-fire icon-white"></i> &nbsp; <font class="home"><%= link_to page.title, page_path(page) %></font></p>
  <% end %>

  <%= link_to 'Add a page', new_page_path, :class => 'btn' %>
  <%= link_to 'Cycle NOC', "/pages/#{@first.id}/cycle", :class => 'btn' %>
</div>
